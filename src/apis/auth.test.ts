// import axios, { AxiosResponse } from "axios"
// import { login } from "./auth.api"

// jest.mock("axios")

// const mockedAxios = axios as jest.Mocked<typeof axios>;

// describe('login', () => {
//     it('should return 200 on success', async () => {
//       mockedAxios.post.mockResolvedValueOnce({ status: 201 } as AxiosResponse);
        
//       const httpStatus = await login();
//       expect(httpStatus).toEqual(201);
//     });
//   });

